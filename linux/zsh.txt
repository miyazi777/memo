●zshを標準シェルにする

(1)zshが使用可能かどうか確認する為に以下のコマンドを打ち込む
cat /etc/shells

使用可能なシェルの一覧が表示されるので、その中にzshがあるかどうか確認
/bin/zsh

(2)現在のログインシェルを確認
echo $SHELL

(3)ログインシェルをzshへ変更する
chsh -s /bin/zsh

(4)ターミナルを再起動する

(5)現在のログインシェルを再確認
echo $SHELL

